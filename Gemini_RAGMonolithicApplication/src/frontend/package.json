{
  "name": "gemini-rag-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.22.3",
    "axios": "^1.6.8",
    "file-saver": "^2.0.5",
    "jspdf": "^2.5.1",
    "styled-components": "^5.3.11",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }

  /* 
    NOTE: 
    After this change, please run 'npm install' in /src/frontend to ensure all missing dependencies, including 'react-scripts', are installed.
    This will fix 'react-scripts: not found' errors and allow 'npm start' to work.
    Required core dependencies for Create React App (CRA):
      - react
      - react-dom
      - react-scripts
    (All are now present.)
  */
}
