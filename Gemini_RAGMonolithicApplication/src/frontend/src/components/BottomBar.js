import React from "react";
import "./BottomBar.css";

// PUBLIC_INTERFACE
// Bottom bar with gradient, fixed to page bottom

const BottomBar = () => (
  <div className="backdrop-bottom-bar"></div>
);

export default BottomBar;
